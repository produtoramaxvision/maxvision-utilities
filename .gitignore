# --- OS ---
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/
.windsurf/

# --- Node.js (defensive - no package.json in this repo) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# --- Secrets / env files ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/

# --- Claude Code ---
.claude/settings.local.json
.claude/memory/
.claude/projects/
CLAUDE.local.md

# --- Local-only scripts (if accidentally placed inside repo) ---
# Sync/update scripts live in the workspace, NOT in this repo
scripts/sync-upstream.sh
scripts/bootstrap.sh
scripts/local-*.sh
*.local.sh

# --- Build artifacts (defensive) ---
dist/
build/
out/
*.log
tmp/
.cache/

# --- Upstream cache (should never be inside repo, but defensive) ---
.upstream-cache/
.upstream/
